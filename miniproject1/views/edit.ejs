<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
        <a  class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
        </div>
    <h5 class="mb-5">You can Edit your post</h5>
     <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind ?" class=" block p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-mb" name="content"><%= post.content %></textarea>
            <input class="px-3 w-40 text-sm py-2 bg-yellow-500 text-black block rounded-mb mt-2" type="Submit" value="Update Your Post">
        </form>
     
</body>
</html>